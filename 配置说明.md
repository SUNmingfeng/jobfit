# JobFit 插件配置说明

## 🔑 配置API密钥

本插件支持多种AI模型（目前支持 **Kimi** 和 **DeepSeek**），使用前需要配置对应的API密钥。

### 配置文件位置

打开 `config.js` 文件，修改 `LLM_CONFIGS` 中的API密钥：

```javascript
LLM_CONFIGS: {
  kimi: {
    name: 'Kimi',
    icon: '🌙',
    description: 'Moonshot AI',
    apiKey: 'sk-your-kimi-api-key-here',  // ← 修改这里
    apiUrl: 'https://api.moonshot.cn/v1/chat/completions',
    model: 'moonshot-v1-8k',
    systemPrompt: '你是一个专业的简历分析助手...'
  },
  deepseek: {
    name: 'DeepSeek',
    icon: '🔮',
    description: '深度求索',
    apiKey: 'sk-your-deepseek-api-key-here',  // ← 修改这里
    apiUrl: 'https://api.deepseek.com/v1/chat/completions',
    model: 'deepseek-chat',
    systemPrompt: '你是一个专业的简历分析助手...'
  }
}
```

---

## 🚀 快速配置

### 方式一：只配置一个模型（推荐新手）

如果只想使用一个模型，只需修改对应的 `apiKey`：

```javascript
kimi: {
  // ...
  apiKey: 'sk-你的实际密钥',
  // ...
}
```

另一个可以保持默认占位符。

### 方式二：配置多个模型

如果需要切换使用多个模型，分别填入对应密钥即可。

---

## 🌙 获取Kimi API密钥

1. 访问 [Moonshot AI官网](https://platform.moonshot.cn/)
2. 点击"立即体验"或"登录"
3. 完成手机号验证注册
4. 进入控制台
5. 点击左侧"API密钥管理"
6. 点击"创建API密钥"
7. 复制生成的密钥到 `kimi.apiKey`

---

## 🔮 获取DeepSeek API密钥

1. 访问 [DeepSeek开放平台](https://platform.deepseek.com/)
2. 点击"立即体验"或注册登录
3. 进入控制台
4. 点击左侧"API Keys"
5. 点击"创建API Key"
6. 复制生成的密钥到 `deepseek.apiKey`

---

## ➕ 添加新模型（扩展）

要添加新的LLM模型（如OpenAI、Claude等），只需在 `LLM_CONFIGS` 中添加配置：

```javascript
LLM_CONFIGS: {
  kimi: { /* 已有配置 */ },
  deepseek: { /* 已有配置 */ },
  
  // 添加新模型示例 - OpenAI GPT
  openai: {
    name: 'OpenAI',
    icon: '🤖',
    description: 'GPT-4',
    apiKey: 'sk-your-openai-key',
    apiUrl: 'https://api.openai.com/v1/chat/completions',
    model: 'gpt-4',
    systemPrompt: '你是一个专业的简历分析助手，擅长提取和结构化简历信息。'
  },
  
  // 添加新模型示例 - Claude
  claude: {
    name: 'Claude',
    icon: '🧠',
    description: 'Anthropic',
    apiKey: 'sk-your-claude-key',
    apiUrl: 'https://api.anthropic.com/v1/messages',
    model: 'claude-3-sonnet-20240229',
    systemPrompt: '你是一个专业的简历分析助手，擅长提取和结构化简历信息。'
  }
}
```

**配置说明：**
- `name`: 模型显示名称
- `icon`: 表情符号图标
- `description`: 简短描述
- `apiKey`: API密钥
- `apiUrl`: API请求地址
- `model`: 具体模型版本
- `systemPrompt`: 系统提示词

**添加后自动生效：**
- 下拉框会自动显示新模型选项
- 无需修改其他代码
- 刷新插件即可使用

---

## 🎯 使用方法

### 在插件界面中选择模型

打开插件后，在**上传简历之前**，可以看到LLM下拉选择框：

```
选择AI模型
┌─────────────────────────────┐
│ 🌙 Kimi                    ▼│
├─────────────────────────────┤
│ 🌙 Kimi                     │
│ 🔮 DeepSeek                 │
│ 🤖 OpenAI (如果已配置)      │
│ 🧠 Claude (如果已配置)      │
└─────────────────────────────┘
当前使用: Kimi
```

### 切换模型
- 点击下拉框选择要使用的模型
- 选择自动保存，下次打开保持选择
- 当前使用的模型名称会显示在下拉框下方

---

## 💡 使用建议

| 模型 | 特点 | 适用场景 |
|------|------|----------|
| **Kimi** | 中文理解强，长文本优秀 | 简历内容较长时推荐 |
| **DeepSeek** | 性价比高，推理能力强 | 追求性价比时推荐 |

---

## ⚠️ 注意事项

1. **API密钥安全**
   - 不要将包含真实API密钥的文件提交到GitHub
   - 不要将插件文件夹分享给他人
   - 建议定期更换API密钥

2. **API费用**
   - 各模型API调用都会产生费用
   - 请关注各自平台的计费标准
   - 建议在控制台设置消费限额

3. **网络要求**
   - 需要网络连接调用API
   - 如果某个API无法访问，可以切换到另一个

---

## 🐛 常见问题

### Q: 提示"API调用失败"
**A**: 
- 检查 `config.js` 中对应模型的 `apiKey` 是否正确
- 检查网络连接
- 登录对应平台查看API密钥状态和余额

### Q: 下拉框中没有显示我添加的模型？
**A**: 
- 刷新插件（访问 `chrome://extensions/` 点击刷新）
- 检查配置格式是否正确（注意逗号、括号）
- 查看Console是否有报错

### Q: 如何设置默认模型？
**A**: 修改 `config.js` 中的 `DEFAULT_LLM`：
```javascript
DEFAULT_LLM: 'deepseek',  // 改成你想要的模型key
```

### Q: 某个模型不想用了如何隐藏？
**A**: 暂时不需要删除配置，只需在界面上不选择即可。如需彻底移除，删除对应的配置块。

---

**配置完成即可开始使用！** 🚀
